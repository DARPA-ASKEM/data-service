name: Correctness
on: [push]

jobs:
  correctness:
    strategy:
      fail-fast: false
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v1

      # Use updated version of SQLite
      - run: |
          wget https://sqlite.org/2022/sqlite-autoconf-3390400.tar.gz
          tar -xvf sqlite-autoconf-3390400.tar.gz
      - run: |
          ./configure
          make
          sudo make install
          export PATH="/usr/local/lib:$PATH"
        working-directory: sqlite-autoconf-3390400

      - uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Run image
        uses: abatilo/actions-poetry@v2
        with:
          poetry-version: 1.2.2
      - name: Create environment
        run:  poetry install
      - name: Pylint source
        run:  poetry run pylint ./tds
      - name: Pylint tests
        run:  poetry run pylint ./tests
      - name: Pytest
        run:  poetry run pytest
